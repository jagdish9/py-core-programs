if __name__ == '__main__':
    data = (1, 2, 3, "Abc", 4.8)

    print(data)

    #data[0] = 4 ## getting error

# Getting error at line no.6 because, tuple is immutable we can not change the value

# We can store any type of data

# Tuple → fixed data, safer, slightly faster